# ---------------------------------------------------------------------------
# QA inventory -- placeholder hosts.
#
# Structurally identical to dev so the same playbooks run unchanged per
# environment. Replace the commented hosts with real QA VMs (or let Terraform
# generate this file). Until then, QA targets no hosts.
# ---------------------------------------------------------------------------

[app]
# app01 ansible_host=10.20.10.11

[database]
# db01 ansible_host=10.20.10.21

[qa:children]
app
database
