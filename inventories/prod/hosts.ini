# ---------------------------------------------------------------------------
# PROD inventory -- placeholder hosts.
#
# In a bank, PROD carries the strictest controls: state access is locked down,
# changes require an approved PR + approved Terraform apply, and runs happen
# from AAP/AWX (never a laptop). Replace the commented hosts with real PROD
# targets or let Terraform generate this file.
# ---------------------------------------------------------------------------

[app]
# app01 ansible_host=10.30.10.11

[database]
# db01 ansible_host=10.30.10.21

[prod:children]
app
database
