# ---------------------------------------------------------------------------
# DEV inventory -- points at the real home-lab VMs (192.168.122.203/.204).
#
# In a bank this file would be *generated* from Terraform outputs
# (see terraform/ + docs/03-hcp-terraform-setup.md). For dev we keep it static
# so the lab runs with zero cloud dependencies.
# ---------------------------------------------------------------------------

[app]
app01 ansible_host=192.168.122.203

[database]
db01 ansible_host=192.168.122.204

# Roll-up group for the whole environment.
[dev:children]
app
database
